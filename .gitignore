__pycache__/
*.pyc
*.egg-info

.venv/
venv/
.DS_Store

# Working directory: downloaded CFTC archives and the SQLite build database.
# Everything the site needs is exported to data/ and committed.
outputs/
_site/

# Local secrets. The API key belongs in the environment, never in config.ini,
# which is committed. See .env.example.
.env
